print ([[

If neither option -check nor option -duplicates is used, the fingerprints for
all sequences are shown on stdout.

Examples:
---------

To be done.

Return values:
--------------

0  everything went fine (if -check was used: the comparison was successful)
1  an error occured     (if -check was used: the comparison was unsuccessful)]])
